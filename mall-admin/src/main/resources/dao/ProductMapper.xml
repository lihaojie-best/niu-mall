<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.niu.mall.admin.dao.ProductMapper">
    <resultMap id="ResultMap" type="com.niu.mall.admin.model.Product">

        <result column="product_id" jdbcType="BIGINT" property="productId" />
        <result column="product_name" jdbcType="VARCHAR" property="productName" />
        <result column="product_price" jdbcType="INTEGER" property="productPrice" />
        <result column="product_sales_volume" jdbcType="INTEGER" property="productSalesVolume" />
        <result column="product_colour" jdbcType="VARCHAR" property="productColour" />
        <result column="product_type" jdbcType="VARCHAR" property="productType" />
        <result column="product_stock" jdbcType="INTEGER" property="productStock" />
        <result column="product_company" jdbcType="VARCHAR" property="productCompany" />
        <result column="product_date" jdbcType="DATE" property="productDate" />
    </resultMap>
    <insert id="create" parameterType="com.niu.mall.admin.model.Product" >
        insert into product(product_id, product_name, product_price,product_sales_volume)
        values (#{productId,jdbcType=BIGINT}, #{productName,jdbcType=VARCHAR},
        #{productPrice,jdbcType=INTEGER},#{productSalesVolume,jdbcType=INTEGER});
        insert into product_attribute (product_id, product_colour, product_type,product_stock)
        values (#{productId,jdbcType=BIGINT}, #{productColour,jdbcType=VARCHAR},#{productType,jdbcType=VARCHAR},#{productStock,jdbcType=INTEGER});
        insert into product_details (product_id, product_company, product_date)
        values (#{productId,jdbcType=BIGINT}, #{productCompany,jdbcType=VARCHAR}, #{productDate,jdbcType=DATE});

    </insert>
    <update id="update" parameterType="com.niu.mall.admin.model.Product">

<set>
    <if test="productName !=null">
        product_name=#{productName,jdbcType=CHAR}
    </if>
</set>
        where product_id = #{productId,jdbcType=BIGINT}
    </update>
   <select id="findById" resultType="com.niu.mall.admin.model.Product">
       select * from product p
            LEFT JOIN product_attribute pa on pa.product_id = p.product_id
           LEFT JOIN product_details pd ON pd.product_id = p.product_id
           where p.product_id=#{productId}
   </select>
    <select id="findAttributeById" resultType="com.niu.mall.admin.model.ProductBasic">
        select * from product_attribute
        where product_id=#{productId}
    </select>
    <select id="updateAttributeById" resultType="com.niu.mall.admin.model.ProductBasic">

    </select>
</mapper>