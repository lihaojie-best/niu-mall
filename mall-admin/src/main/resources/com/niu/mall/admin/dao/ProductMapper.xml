<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.niu.mall.admin.dao.ProductDao">
    <resultMap id="ResultMap" type="com.niu.mall.admin.domain.Product">
        <id column="product_id" jdbcType="BIGINT" property="productId" />
        <result column="product_name" jdbcType="VARCHAR" property="productName" />
        <result column="product_price" jdbcType="INTEGER" property="productPrice" />
        <result column="product_sales_volume" jdbcType="INTEGER" property="productSalesVolume" />
        <result column="product_colour" jdbcType="VARCHAR" property="productColour" />
        <result column="product_type" jdbcType="VARCHAR" property="productType" />
        <result column="product_stock" jdbcType="INTEGER" property="productStock" />
        <result column="product_company" jdbcType="VARCHAR" property="productCompany" />
    </resultMap>
    <insert id="createProduct" parameterType="com.niu.mall.admin.domain.Product" useGeneratedKeys ="true" keyProperty = "productId" >
        INSERT INTO `product`( `product_name`, `product_colour`,`product_company`, `product_type`,
                               `product_price`,`product_sales_volume`,`product_stock`)
        VALUES (#{productName},#{productColour},#{productCompany},#{productType},#{productPrice},#{productSalesVolume},#{productStock});

    </insert>
    <update id="update" parameterType="com.niu.mall.admin.domain.Product">
        update `product`
        <set>
            <if test="productName !=null">
                `product_name`=#{productName},
            </if>
            <if test="productColour !=null">
                `product_colour`=#{productColour},
            </if>
            <if test="productCompany !=null">
                `product_company`=#{productCompany},
            </if>
            <if test="productType !=null">
                `product_type`=#{productType},
            </if>
            <if test="productPrice !=null">
                `product_price`=#{productPrice},
            </if>
            <if test="productSalesVolume !=null">
                `product_sales_volume`=#{productSalesVolume},
            </if>
            <if test="productStock !=null">
                `product_stock`=#{productStock},
            </if>
        </set>
        where `product_id` = #{productId}
    </update>
    <select id="findById"  resultMap="ResultMap" parameterType="long">
        select * from `product` where product_id=#{productId};
    </select>
</mapper>
